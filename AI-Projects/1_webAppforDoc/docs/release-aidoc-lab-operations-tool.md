# Lab operations tool — AI-Generated Documentation

**Generated:** 2026-06-22 15:38:26
**Source:** `C:\Users\du974f\Desktop\1_LabOps`
**Files Analyzed:** 23
**AI Provider:** Google Gemini (FREE)

---

This is a FRONTEND project.

## 1. Project Setup

### How to install and run the app

1.  **Install Node.js and npm:** Ensure you have Node.js and npm (Node Package Manager) installed on your system. You can download them from [nodejs.org](https://nodejs.org/).
2.  **Clone the repository:**
    ```bash
    git clone <repository_url>
    cd <project_directory>
    ```
3.  **Install dependencies:** Navigate to the project's root directory and run:
    ```bash
    npm install
    ```
    or
    ```bash
    yarn install
    ```
4.  **Start the development server:**
    ```bash
    npm start
    ```
    or
    ```bash
    yarn start
    ```
    This command will start the development server, and the application will typically be available at `http://localhost:3000/` in your web browser.

### Configuration needed

*   **Environment Variables:** This project uses `react-scripts` which supports environment variables prefixed with `REACT_APP_`. However, based on the provided code, no specific environment variables are explicitly configured or used within the application's logic. The `manifest.yml` file contains `PUBLIC_URL: /default` which might influence the base path of the application when deployed.

## 2. Application Modules (Pages)

*   **`App.js`**: The main application component that sets up the overall shell, including the header, sidebar, and the main content area which dynamically renders different modules based on the selected tab.
*   **`CalendarWithBookingLatest`**: This module is the core of the lab operations tool. It displays a calendar interface using `@fullcalendar/react` to show booked slots and available time slots for lab resources. It allows users to filter bookings and select time slots to create new bookings.
*   **`Header.js`**: Renders the top navigation bar of the application, including the company logo, application title, and a user account/profile menu.
*   **`Sidebar.js`**: Renders the left-hand navigation menu, allowing users to switch between different sections of the application (e.g., "calendar").
*   **`Modal.js`**: A reusable modal component used for displaying booking details and for creating new bookings. It contains forms for inputting booking information. (Note: The full code for `Modal.js` was truncated, so its specific functionality is inferred from its usage).

## 3. For EACH Module:

### `App.js`

*   **What it does:** This is the root component of the React application. It orchestrates the main layout, including the header, sidebar, and the primary content area. It manages the currently selected tab from the sidebar and renders the corresponding module.
*   **Features:**
    *   **Layout Structure:** Renders a main application shell with a `Header`, `Sidebar`, and a `main` content area.
    *   **Tab Management:** Uses `useState` to manage the `selectedTab` state. When a tab is selected in the `Sidebar`, this state is updated, causing the `main` content area to re-render with the appropriate component.
    *   **Dynamic Content Rendering:** Conditionally renders `CalendarWithBookingLatest` if `selectedTab` is "calendar", otherwise displays a placeholder div with the selected tab's name and sample data.
    *   **Account Toggle (Demo):** Includes a placeholder `handleToggleAccount` function that shows an alert, indicating where user authentication or account management logic would be integrated.
*   **Components used:**
    *   `Header`
    *   `Sidebar`
    *   `CalendarWithBookingLatest`
*   **API calls:** None directly within this component.
*   **State/Data:**
    *   `selectedTab`: String, stores the ID of the currently active tab (e.g., "calendar").

### `CalendarWithBookingLatest`

*   **What it does:** This module provides a comprehensive calendar interface for managing lab operations bookings. It displays pre-booked slots, calculates and displays available time slots, and allows users to select time ranges to create new bookings. It also supports filtering of bookings based on various criteria.
*   **Features:**
    *   **Calendar Display:** Integrates `@fullcalendar/react` to render a week-view calendar.
    *   **Booking and Availability Visualization:**
        *   **Booked Slots:** Renders pre-booked slots with distinct background colors based on priority (High, Medium, Low, Default).
        *   **Available Slots:** Computes and displays available time slots as background events with a specific green color and opacity. These are generated by finding gaps between booked slots within working hours.
    *   **Filtering:** Provides dropdowns for filtering bookings by:
        *   Facility
        *   Asset (type)
        *   Program (event type)
        *   Priority
        The calendar dynamically updates to show only bookings matching the selected filters.
    *   **Slot Selection and Booking:**
        *   Users can select a time range on the calendar by clicking and dragging.
        *   If all filter dropdowns are selected, a modal appears to confirm and finalize the booking.
        *   The system checks for conflicts with existing bookings before allowing a new booking.
        *   New bookings are added to `preBookedSlots` state and rendered on the calendar.
    *   **Modal Interaction:**
        *   Opens a `Modal` component when a user selects an available time slot or clicks on a booked slot.
        *   The modal displays booking details (for booked slots) or allows input for creating a new booking (for available slots).
        *   Supports deleting bookings (via `handleDeleteBooking`).
    *   **Snackbar Notifications:** Uses MUI `Snackbar` and `Alert` components to provide user feedback for actions like successful bookings, errors, or warnings.
    *   **Dynamic Asset Filtering:** The "Asset" dropdown is dynamically populated based on the selected "Facility".
    *   **Past Slot Prevention:** Prevents users from selecting or booking time slots in the past.
    *   **Event Content Customization:** Customizes the display of calendar events to show title, priority, and "Booked" status.
*   **Components used:**
    *   `FullCalendar` (from `@fullcalendar/react`)
    *   `Modal`
    *   `Snackbar`, `Alert` (from `@mui/material`)
*   **API calls:** None directly within this component. Data is loaded from local JSON files (`eventTypes.json`, `facilities.json`, `assetsByFacility.json`, `preBookedSlots.json`, `typeOptions.json`).
*   **State/Data:**
    *   `modalOpen`: Boolean, controls the visibility of the booking modal.
    *   `selectedStart`, `selectedEnd`: Date objects, store the start and end times of the selected slot for booking.
    *   `events`: Array of event objects, representing user-added bookings.
    *   `asset`: String, the currently selected asset (type).
    *   `program`: String, the currently selected program (event type).
    *   `facility`: String, the currently selected facility.
    *   `priority`: String, the currently selected priority for booking.
    *   `preBookedSlots`: Array of objects, representing pre-booked slots loaded from JSON and potentially updated by user actions.
    *   `viewOnlyModalData`: Object, stores data for displaying booked slots in a read-only modal.
    *   `snackbar`: Object, manages the state of the Snackbar notification.
    *   `WORK_START`, `WORK_END`, `SLOT_MINUTES`, `HORIZON_MONTHS`: Constants for calendar configuration.

### `Header.js`

*   **What it does:** Renders the application's header bar, which includes the company logo, the application title, and a user profile/account menu.
*   **Features:**
    *   **Logo Display:** Shows the "Boeing" logo.
    *   **Application Title:** Displays "Lab Operation Scheduler - LabOps".
    *   **User Account Menu:**
        *   A button with a person icon triggers a dropdown menu.
        *   The menu displays the user's name and email (hardcoded for demo).
        *   Includes "View profile" and "Sign out" (or "Sign in") buttons, which trigger alert messages for demonstration.
    *   **Dropdown Logic:** Manages the open/closed state of the account menu using `useState` and handles clicks outside the menu to close it.
    *   **Keyboard Navigation:** Supports closing the menu with the "Escape" key.
*   **Components used:**
    *   `PersonOutlineIcon` (from `@mui/icons-material`)
    *   `BoeingLogo` (imported image)
*   **API calls:** None.
*   **State/Data:**
    *   `open`: Boolean, controls the visibility of the account dropdown menu.
    *   `signedIn`: Boolean, indicates user login status (hardcoded `true`).
    *   `user`: Object, contains hardcoded user details (`name`, `email`).

### `Sidebar.js`

*   **What it does:** Renders the main navigation sidebar for the application. It contains a list of navigation items, and clicking on them updates the application's main view.
*   **Features:**
    *   **Navigation Items:** Displays a list of navigation links, such as "Calendar".
    *   **Active State Indication:** Highlights the currently active navigation item.
    *   **Tab Selection:** When a navigation item is clicked, it calls the `onSelect` prop (passed from `App.js`) with the ID of the selected item.
*   **Components used:** None directly imported, uses basic HTML elements and inline styles.
*   **API calls:** None.
*   **State/Data:**
    *   `activeId`: String, stores the ID of the currently active sidebar item.

## 4. Shared Components

*   **`Modal.js`**:
    *   **What it does:** A generic modal component used for displaying information or collecting user input. It's used in `CalendarWithBookingLatest` for both viewing booking details and creating new bookings. It accepts props for start/end times, submission handlers, default values, and data for view-only mode.
    *   **Features:**
        *   Displays a title, content area, and action buttons (e.g., Submit, Close, Delete).
        *   Handles form inputs for booking details (program, asset, facility, priority).
        *   Can be configured for view-only mode.
        *   Accepts `defaults` to pre-fill form fields.
        *   Accepts `onSubmit` and `onClose` callbacks.
        *   Accepts `onDelete` callback for cancelling bookings.
        *   Receives options for dropdowns like `eventTypes`, `assetOptions`, `facilities`.
*   **`BoeingLogo` (Image):**
    *   **What it does:** The company logo image used in the `Header.js` component.
*   **`PersonOutlineIcon` (Material-UI Icon):**
    *   **What it does:** An icon component from Material-UI, used in `Header.js` for the user avatar and menu button.

## 5. State Management / Stores

*   **`useState` Hook:** The primary mechanism for managing local component state within individual modules.
    *   **`App.js`:** Manages `selectedTab` to control the displayed content.
    *   **`CalendarWithBookingLatest.js`:** Manages `modalOpen`, `selectedStart`, `selectedEnd`, `events`, `asset`, `program`, `facility`, `priority`, `preBookedSlots`, `viewOnlyModalData`, and `snackbar` for the calendar and booking functionality.
    *   **`Header.js`:** Manages `open` for the account menu dropdown.
    *   **`Sidebar.js`:** Manages `activeId` to highlight the active navigation item.
*   **JSON Data Files:** Static data for dropdowns and initial bookings are loaded from JSON files (`eventTypes.json`, `facilities.json`, `assetsByFacility.json`, `preBookedSlots.json`, `typeOptions.json`). This data is not managed by a global state store but is fetched and used within the `CalendarWithBookingLatest` module.
*   **No Global State Management Library:** There is no explicit use of a global state management library like Redux or Zustand. State is managed locally within components or passed down via props. For instance, the `selectedTab` state in `App.js` is passed down to `Sidebar.js` via a prop (`onSelect`).

---

*AI-generated documentation — 2026-06-22 15:38:26*
*Review for accuracy. AI may misinterpret complex logic.*
